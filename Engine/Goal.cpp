#include "Goal.h"



Goal::Goal()
	
{/*
	std::random_device rd;
	std::mt19937 rng( rd() );
	std::uniform_int_distribution<int> xDist(0,80);
	std::uniform_int_distribution<int> yDist(0,60);
	loc.x = xDist( rng );
	loc.y = yDist( rng );*/
	//loc.x = rand() %80;
//	loc.y = rand() %60;	
	
}



Goal::~Goal()
{
}
